"use client";import {useState} from "react";import {Shell} from "../../components/shell";import {MapDesk} from "../../components/map-desk";
export default function Report(){const [saved,setSaved]=useState(false);return <Shell><MapDesk title="Field report"/><section className="sheet report"><p className="eyebrow">OFF-CHAIN FIELD NOTE</p><h1>Record an observation</h1><p>This form prepares a local, non-authoritative report. Publish a public immutable evidence bundle before submitting a cluster to the contract.</p><label>Feature key<input placeholder="e.g. harbor-pharmacy"/></label><label>Observed change<select><option>Status → CLOSED</option><option>Name</option><option>Access</option><option>Category</option></select></label><label>Public evidence URL<input type="url" placeholder="https://…"/></label><label>Field note<textarea placeholder="Describe only what a public source supports."/></label><button onClick={()=>setSaved(true)}>Save field report</button>{saved&&<p className="notice">Saved in this browser session only. It is not canonical map state.</p>}</section></Shell>}
