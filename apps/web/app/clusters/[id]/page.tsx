"use client";import {useEffect,useState} from "react";import {Shell} from "../../../components/shell";import {Unavailable} from "../../../components/live";import {TransactionRail} from "../../../components/transaction-rail";import {contractWrite} from "../../../lib/genlayer/contract";import {getCluster,related} from "../../../lib/genlayer/data-source";import type {Cluster,Related,TxStage} from "../../../lib/genlayer/types";
export default function ClusterPage({params}:{params:Promise<{id:string}>}){const [id,setId]=useState("");const [cluster,setCluster]=useState<Cluster>();const [memories,setMemories]=useState<Related[]>([]);const [error,setError]=useState("");const [stage,setStage]=useState<TxStage>("idle");const [hash,setHash]=useState("");useEffect(()=>{params.then(async p=>{setId(p.id);try{setCluster(await getCluster(p.id));setMemories(await related(p.id));}catch(e){setError(e instanceof Error?e.message:"Unavailable")}})},[params]);async function adjudicate(){try{await contractWrite("adjudicate_cluster",[id],(s,h,m)=>{setStage(s);setHash(h??"");if(m)setError(m)});setCluster(await getCluster(id));}catch(e){setError(e instanceof Error?e.message:"Write failed")}}return <Shell><TransactionRail stage={stage} hash={hash} message={error}/>{error&&!cluster?<Unavailable message={error}/>:cluster?<section className="compare"><div className="evidence"><p className="eyebrow">IMMUTABLE EVIDENCE</p><a href={cluster.bundle_url}>{cluster.bundle_url}</a><code>{cluster.bundle_digest}</code><p>Bound to feature version {cluster.base_version} · {cluster.geohash}</p></div><div className="decision"><p className="eyebrow">CLUSTER {id}</p><h1>One feature, one attribute.</h1><p>{cluster.rationale||"Awaiting semantic adjudication of bounded public evidence."}</p><button onClick={adjudicate} disabled={stage!=="idle"}>Adjudicate on StudioNet</button></div><aside className="memory"><h2>Related records retrieved</h2>{memories.length?memories.map(m=><div key={m.delta_id}><strong>Delta {m.delta_id}</strong><small>raw distance {m.distance}</small></div>):<p>No eligible semantic memory found.</p>}</aside><footer><span>BEFORE</span><b>{cluster.attribute}</b><i>→</i><b>{cluster.value}</b><span>PROPOSED</span></footer></section>:null}</Shell>}
