import type {TxStage} from "../lib/genlayer/types"; export function TransactionRail({stage,hash,message}:{stage:TxStage;hash?:string;message?:string}){if(stage==="idle")return null;const copy=stage==="signing"?"Awaiting wallet signature":stage==="submitted"?"Submitted":stage==="pending"?"Consensus / finality pending":stage==="success"?"Finalized; GenVM execution successful":stage==="rollback"?"Finalized; GenVM execution rolled back":"Transaction failed";return <aside className={'tx '+stage}><strong>{copy}</strong>{hash&&<code>{hash}</code>}{message&&<span>{message}</span>}</aside>}
